export function SiteFooter() {
  return (
    <footer className="border-t border-border">
      <div className="mx-auto flex max-w-6xl flex-col items-center justify-between gap-3 px-4 py-8 text-sm text-faint sm:flex-row sm:px-6">
        <p>
          <span className="font-mono text-muted">invokenotes</span> —
          คู่มือสำรวจ agent skills
        </p>
        <p>ข้อมูลตัวอย่าง · คัดสรรเพื่อความเข้าใจ ไม่ใช่การรับรอง</p>
      </div>
    </footer>
  );
}
